{
  "name": "@caido-backslash/cli",
  "version": "0.2.0",
  "description": "Run the backslash engine against a target without Caido. Byte-exact sending over raw sockets.",
  "type": "module",
  "bin": {
    "backslash": "src/main.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@caido-backslash/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "typescript": "5.9.2",
    "vitest": "3.2.3"
  }
}
